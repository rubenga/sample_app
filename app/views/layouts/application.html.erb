<!DOCTYPE html>
<html>
  <head>
    <title><%= titre %></title> <!-- défini dans application_helper-->
    <%= csrf_meta_tag %>
    <%= render 'layouts/stylesheets' %> <!-- partials-->
  </head>
  <body>
    <div class="container">
      <%= render 'layouts/header' %> <!-- partials-->
      <section class="round">
        <%= yield %>
      </section>
      <%= render 'layouts/footer' %> <!-- partials-->
    </div>
  </body>
</html>
